import React from "react";
import ProjectsLayout from "@/components/layouts/projects-layout";

export const metadata = {
  title: "Projects | Digixito",
  description: "Crafted with precision. Explore our recent client work.",
};

const ProjectsPage = () => {
  return (
    <main>
      <ProjectsLayout />
    </main>
  );
};

export default ProjectsPage;
